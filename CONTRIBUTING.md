# 🤝 Руководство по внесению вклада

Спасибо за интерес к Equipment Tracker! Мы приветствуем вклад от сообщества.

## Кодекс поведения

Участвуя в этом проекте, вы соглашаетесь соблюдать:
- Уважительное отношение к другим участникам
- Конструктивная критика
- Фокус на том, что лучше для проекта
- Эмпатия к другим членам сообщества

## Как внести вклад

### 1. Сообщение об ошибках

Перед созданием Issue:
- Проверьте, нет ли уже подобного Issue
- Используйте шаблон Issue (если есть)
- Опишите проблему детально

**Хороший отчёт об ошибке включает:**
- Краткое описание проблемы
- Шаги для воспроизведения
- Ожидаемое поведение
- Фактическое поведение
- Скриншоты (если применимо)
- Версию приложения
- Операционную систему
- Логи (если есть)

**Пример:**
```
**Описание:**
Приложение вылетает при попытке экспорта CSV с большим количеством записей

**Шаги воспроизведения:**
1. Создать 1000+ записей выходов
2. Перейти в раздел "Выходы сотрудников"
3. Нажать "Экспортировать в CSV"

**Ожидается:**
Открывается диалог сохранения файла

**Фактически:**
Приложение закрывается без ошибок

**Версия:** 1.0.18
**ОС:** Windows 10 Pro
**Логи:** см. приложенный файл
```

### 2. Предложение функций

Перед предложением новой функции:
- Проверьте существующие Issues и PR
- Обсудите идею в Discussions (если есть)
- Объясните зачем нужна функция

**Хорошее предложение включает:**
- Описание проблемы, которую решает функция
- Предлагаемое решение
- Альтернативы, которые вы рассматривали
- Примеры использования
- Mockup/скриншоты (если применимо)

### 3. Pull Requests

#### Процесс

1. **Fork репозитория**
   ```bash
   # На GitHub: нажмите Fork
   ```

2. **Клонируйте свой fork**
   ```bash
   git clone https://github.com/ваш-username/Equipment.Tracker.git
   cd Equipment.Tracker
   ```

3. **Создайте ветку**
   ```bash
   git checkout -b feature/amazing-feature
   # или
   git checkout -b fix/bug-description
   ```

4. **Сделайте изменения**
   - Следуйте [стилю кода](#стиль-кода)
   - Добавьте комментарии для сложной логики
   - Обновите документацию при необходимости

5. **Закоммитьте изменения**
   ```bash
   git add .
   git commit -m "feat: добавлена новая функция"
   ```
   
   Используйте [Conventional Commits](#conventional-commits)

6. **Push в свой fork**
   ```bash
   git push origin feature/amazing-feature
   ```

7. **Создайте Pull Request**
   - Дайте понятное название
   - Опишите что изменилось и почему
   - Сошлитесь на связанные Issues
   - Приложите скриншоты (для UI изменений)

#### Требования к PR

- [ ] Код проходит `npm run lint`
- [ ] Код проходит `npm run format:check`
- [ ] Приложение собирается без ошибок
- [ ] Функциональность протестирована вручную
- [ ] Документация обновлена (если нужно)
- [ ] Миграции БД созданы (если нужно)
- [ ] Нет breaking changes (или они документированы)

#### Что проверяют в PR

1. **Качество кода**
   - TypeScript типизация
   - Соответствие ESLint правилам
   - Форматирование Prettier

2. **Архитектура**
   - Следование архитектурным принципам
   - Правильное использование IPC
   - Безопасность (Context Isolation, валидация)

3. **Функциональность**
   - Работает ли код как ожидается
   - Нет ли регрессии
   - Покрыты ли edge cases

4. **UX**
   - Понятные сообщения об ошибках
   - Loading states
   - Responsive дизайн

5. **Документация**
   - Обновлены ли соответствующие .md файлы
   - Есть ли комментарии для сложной логики

## Стиль кода

### TypeScript

```typescript
// ✅ Хорошо
interface User {
  id: number
  name: string
  email: string
}

function getUserById(id: number): User | null {
  // Реализация
}

// ❌ Плохо
function getUserById(id: any): any {
  // Реализация
}
```

### React компоненты

```typescript
// ✅ Хорошо
interface MyComponentProps {
  title: string
  onClose: () => void
}

export function MyComponent({ title, onClose }: MyComponentProps) {
  return (
    <div className="space-y-4">
      <h2>{title}</h2>
      <Button onClick={onClose}>Закрыть</Button>
    </div>
  )
}

// ❌ Плохо
export function MyComponent(props: any) {
  return <div>{props.title}</div>
}
```

### Именование

```typescript
// Компоненты: PascalCase
function UserProfile() {}

// Функции и переменные: camelCase
const getUserData = () => {}
const userName = 'John'

// Константы: UPPER_SNAKE_CASE
const MAX_RETRIES = 3
const API_ENDPOINT = '/api'

// Файлы компонентов: PascalCase.tsx
UserProfile.tsx

// Файлы утилит: kebab-case.ts
user-utils.ts
```

### Организация импортов

```typescript
// 1. React и внешние библиотеки
import React from 'react'
import { useQuery } from '@tanstack/react-query'

// 2. Внутренние модули
import { useRequests } from '@/hooks/useRequests'
import { Button } from '@/components/ui/button'

// 3. Типы
import type { Request } from '@/types/ipc'

// 4. Стили (если есть)
import './styles.css'
```

### Комментарии

```typescript
// ✅ Хорошо - объясняет "почему"
// Используем setTimeout для отложенного сохранения,
// чтобы избежать множественных записей в БД
const debouncedSave = useMemo(() => 
  debounce(saveData, 300)
, [])

// ❌ Плохо - объясняет "что" (очевидно из кода)
// Создаём переменную user
const user = getUser()
```

## Conventional Commits

Используйте семантические коммиты:

```bash
# Новая функция
git commit -m "feat: добавлена функция экспорта в CSV"

# Исправление бага
git commit -m "fix: исправлена ошибка при удалении заявки"

# Документация
git commit -m "docs: обновлена документация API"

# Рефакторинг
git commit -m "refactor: упрощена логика валидации форм"

# Производительность
git commit -m "perf: оптимизирован поиск по заявкам"

# Стили/форматирование
git commit -m "style: исправлено форматирование в RequestsView"

# Тесты
git commit -m "test: добавлены тесты для useRequests hook"

# Сборка/конфигурация
git commit -m "build: обновлены зависимости Electron"

# CI/CD
git commit -m "ci: добавлена проверка типов в GitHub Actions"
```

### Scope (опционально)

```bash
git commit -m "feat(requests): добавлен фильтр по статусу"
git commit -m "fix(database): исправлена миграция equipment_items"
git commit -m "docs(api): обновлена документация IPC методов"
```

### Breaking Changes

```bash
git commit -m "feat!: изменён формат equipment_list на JSON

BREAKING CHANGE: equipment_list теперь всегда JSON массив.
Старые текстовые записи автоматически конвертируются при миграции."
```

## Тестирование

### Ручное тестирование

Перед отправкой PR протестируйте:

#### Основная функциональность
- [ ] Создание записи
- [ ] Редактирование записи
- [ ] Удаление записи
- [ ] Поиск работает
- [ ] Фильтры работают

#### UI/UX
- [ ] Loading states отображаются
- [ ] Toast уведомления работают
- [ ] Валидация форм корректна
- [ ] Тёмная тема работает
- [ ] Responsive на разных размерах экрана

#### Данные
- [ ] Данные сохраняются в БД
- [ ] Данные загружаются корректно
- [ ] Нет потери данных
- [ ] Миграции работают

#### Кросс-платформенность
- [ ] Windows (обязательно)
- [ ] macOS (если доступно)
- [ ] Linux (если доступно)

### Автоматизированное тестирование

```bash
# Линтинг
npm run lint

# Проверка типов
npx tsc --noEmit

# Форматирование
npm run format:check

# Сборка
npm run build:bundle
```

## Структура проекта

При добавлении файлов следуйте структуре:

```
equipment-tracker/
├── electron/              # Main Process
│   ├── ipc/              # Новые IPC handlers сюда
│   ├── main.ts           # Регистрация handlers
│   └── database.ts       # DB схема
│
├── src/                  # Renderer Process
│   ├── components/       # React компоненты
│   │   ├── ui/          # shadcn/ui примитивы
│   │   ├── *View.tsx    # Главные экраны
│   │   └── *Modal.tsx   # Модальные окна
│   ├── hooks/           # Custom React hooks
│   ├── types/           # TypeScript типы
│   └── lib/             # Утилиты
│
└── docs/                # Документация
```

## Работа с Issues

### Выбор Issue

1. Найдите Issue с меткой `good first issue` для начала
2. Проверьте, что Issue не назначен на другого
3. Оставьте комментарий, что берёте задачу

### Процесс работы

1. Создайте ветку от `main`
2. Разработайте решение
3. Протестируйте
4. Создайте PR со ссылкой на Issue

### Коммуникация

- Обновляйте Issue о прогрессе
- Задавайте вопросы в комментариях
- Если не можете завершить — сообщите

## Review процесс

### Что ожидать

1. **Автоматические проверки**
   - Линтинг
   - Форматирование
   - Сборка

2. **Code review**
   - Проверка кода
   - Архитектурные вопросы
   - Предложения по улучшению

3. **Тестирование**
   - Функциональность
   - UX
   - Производительность

### Обработка feedback

- Не принимайте критику на личный счёт
- Задавайте вопросы, если что-то непонятно
- Обсуждайте альтернативные решения
- Вносите запрошенные изменения

## Релизы

Релизы создаются maintainers согласно [семантическому версионированию](https://semver.org/):

- **MAJOR** (1.0.0 → 2.0.0): Breaking changes
- **MINOR** (1.0.0 → 1.1.0): Новая функциональность
- **PATCH** (1.0.0 → 1.0.1): Исправления багов

## Лицензия

Внося вклад в проект, вы соглашаетесь, что ваш код будет распространяться под MIT лицензией.

## Вопросы?

- 📖 Прочитайте [документацию](./docs/)
- 🐛 Создайте [Issue](https://github.com/MaRT1n1q/Equipment.Tracker/issues)
- 💬 Обсудите в [Discussions](https://github.com/MaRT1n1q/Equipment.Tracker/discussions)

## Благодарности

Спасибо всем, кто вносит вклад в Equipment Tracker! 🎉

### Contributors

<!-- ALL-CONTRIBUTORS-LIST:START -->
<!-- Автоматически генерируется через all-contributors -->
<!-- ALL-CONTRIBUTORS-LIST:END -->

---

**Ещё раз спасибо за ваш вклад! 🙏**
