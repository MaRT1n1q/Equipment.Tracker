# Инструменты разработки

Этот документ описывает все установленные инструменты для разработки и контроля качества кода.

## 🔧 Установленные инструменты

### 1. **Commitlint** ✅

Проверяет формат сообщений коммитов по стандарту Conventional Commits.

**Конфигурация**: `commitlint.config.js`

**Допустимые типы коммитов:**

- `feat:` — новая функциональность
- `fix:` — исправление бага
- `docs:` — изменения в документации
- `style:` — форматирование, отступы
- `refactor:` — рефакторинг кода
- `perf:` — улучшение производительности
- `test:` — добавление/изменение тестов
- `build:` — изменения в сборке
- `ci:` — изменения в CI/CD
- `chore:` — прочие изменения
- `revert:` — откат изменений

**Примеры:**

```bash
git commit -m "feat: добавлена возможность экспорта в Excel"
git commit -m "fix: исправлена ошибка при сохранении заявки"
git commit -m "docs: обновлена документация API"
```

### 2. **Vitest** ✅

Фреймворк для тестирования с поддержкой React компонентов.

**Конфигурация**: `vitest.config.ts`

**Команды:**

```bash
npm run test          # Запуск в watch режиме
npm run test:run      # Однократный запуск
npm run test:ui       # UI интерфейс для тестов
npm run test:coverage # Отчет о покрытии кода
```

**Примеры тестов:**

- `src/lib/employeeExitEquipment.test.ts`
- `src/lib/utils.test.ts`

**Создание новых тестов:**
Создайте файл `*.test.ts` рядом с тестируемым файлом.

### 3. **Dependabot** ✅

Автоматически создает PR с обновлениями зависимостей.

**Конфигурация**: `.github/dependabot.yml`

**Расписание**: каждый понедельник в 09:00

**Что обновляет:**

- npm зависимости (группирует minor и patch обновления)
- GitHub Actions

### 4. **Pre-push хук** ✅

Автоматические проверки перед `git push`.

**Файл**: `.husky/pre-push`

**Что проверяет:**

1. ESLint — ошибки линтинга
2. Тесты — все тесты должны пройти
3. Сборка — код должен собираться без ошибок

**Отключение** (не рекомендуется):

```bash
git push --no-verify
```

### 5. **Release-it** ✅

Автоматизация создания релизов с генерацией CHANGELOG.

**Конфигурация**: `.release-it.json`

**Команды:**

```bash
npm run release        # Patch версия (1.0.0 -> 1.0.1)
npm run release:minor  # Minor версия (1.0.0 -> 1.1.0)
npm run release:major  # Major версия (1.0.0 -> 2.0.0)
```

**Что делает:**

1. Запускает lint и тесты
2. Обновляет версию в `package.json`
3. Генерирует `CHANGELOG.md`
4. Создает git тег
5. Создает GitHub Release
6. Собирает приложение

**Требования:**

- Чистый git (нет незакоммиченных изменений)
- Ветка `main`
- Переменная окружения `GITHUB_TOKEN`

## 🎯 Git хуки

### Pre-commit

Файл: `.husky/pre-commit`

**Что делает:**

- Запускает `lint-staged`
- Проверяет только staged файлы
- ESLint с автофиксом
- Prettier проверка форматирования

### Commit-msg

Файл: `.husky/commit-msg`

**Что делает:**

- Проверяет формат сообщения коммита
- Блокирует коммиты с неправильным форматом

### Pre-push

Файл: `.husky/pre-push`

**Что делает:**

- Полная проверка всего проекта
- Запускает линтер, тесты, сборку
- Блокирует push если есть ошибки

## 📊 Workflow разработки

### Ежедневная работа

1. **Создание новой ветки:**

```bash
git checkout -b feat/новая-фича
```

2. **Работа над кодом:**

- Пишите код
- Добавляйте тесты для новой функциональности
- Запускайте `npm run test` локально

3. **Коммит изменений:**

```bash
git add .
git commit -m "feat: описание изменений"
```

Husky автоматически проверит код.

4. **Push в удаленный репозиторий:**

```bash
git push
```

Pre-push хук запустит все проверки.

### Создание релиза

1. **Слияние изменений в main:**

```bash
git checkout main
git merge feat/новая-фича
```

2. **Создание релиза:**

```bash
export GITHUB_TOKEN=ваш-токен
npm run release
```

3. **Release-it автоматически:**

- Проверит код
- Обновит версию
- Создаст CHANGELOG
- Запушит изменения
- Создаст GitHub Release

## 🧪 Тестирование

### Написание тестов

**Для утилит:**

```typescript
import { describe, it, expect } from 'vitest'
import { myFunction } from './myFile'

describe('myFunction', () => {
  it('должна делать что-то', () => {
    const result = myFunction('input')
    expect(result).toBe('expected')
  })
})
```

**Для React компонентов:**

```typescript
import { render, screen } from '@testing-library/react';
import { describe, it, expect } from 'vitest';
import MyComponent from './MyComponent';

describe('MyComponent', () => {
  it('должен отрендериться', () => {
    render(<MyComponent />);
    expect(screen.getByText('Текст')).toBeInTheDocument();
  });
});
```

### Покрытие кода

```bash
npm run test:coverage
```

Отчет сохраняется в `coverage/index.html`.

## 🔍 Отладка

### Проблемы с хуками

Если хуки не работают:

```bash
npx husky install
chmod +x .husky/*
```

### Пропуск проверок (только для экстренных случаев)

```bash
# Пропустить pre-commit
git commit --no-verify

# Пропустить pre-push
git push --no-verify
```

### Проблемы с тестами

```bash
# Очистить кеш
npm run test:run -- --clearCache

# Запустить конкретный тест
npm run test -- src/lib/utils.test.ts
```

## 📚 Дополнительная документация

- [Commitlint](https://commitlint.js.org/)
- [Vitest](https://vitest.dev/)
- [Release-it](https://github.com/release-it/release-it)
- [Dependabot](https://docs.github.com/en/code-security/dependabot)

## ✅ Чеклист для новых разработчиков

- [ ] Клонировать репозиторий
- [ ] Запустить `npm install`
- [ ] Проверить работу тестов: `npm run test:run`
- [ ] Сделать тестовый коммит для проверки хуков
- [ ] Прочитать этот документ полностью

## 🎓 Лучшие практики

1. **Всегда пишите тесты** для новой функциональности
2. **Используйте правильный тип коммита** (feat/fix/docs/etc)
3. **Не игнорируйте ошибки линтера** — исправляйте их
4. **Запускайте тесты локально** перед push
5. **Проверяйте coverage** — стремитесь к 80%+
6. **Просматривайте PR от Dependabot** — держите зависимости актуальными
