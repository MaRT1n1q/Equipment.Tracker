# 🏗️ Архитектура Equipment Tracker

## Обзор

Equipment Tracker — это десктопное приложение на базе Electron, предназначенное для учёта выдачи оборудования сотрудникам и управления их выходами. Приложение использует оффлайн-первый подход с локальной SQLite базой данных.

## Технологический стек

### Фронтенд (Renderer Process)

- **React 19** — основная библиотека UI
- **TypeScript 5** — строгая типизация
- **Vite 7** — сборщик и dev-сервер
- **TanStack Query 5** — управление серверным состоянием
- **Tailwind CSS 3** — утилитарный CSS фреймворк
- **shadcn/ui** — коллекция UI компонентов
- **Lucide React** — набор иконок
- **Sonner** — toast-уведомления

### Бэкенд (Main Process)

- **Electron 39** — десктопная оболочка
- **SQLite 3** — локальная база данных
- **Knex.js** — query builder для работы с БД
- **Zod** — валидация схем данных
- **electron-updater** — автоматические обновления
- **electron-log** — логирование

### Инструменты разработки

- **ESLint** — линтер кода
- **Prettier** — форматирование кода
- **Husky** — git hooks
- **lint-staged** — проверка staged файлов

## Архитектурные принципы

### 1. Разделение процессов

Приложение следует архитектуре Electron с четким разделением:

```
┌─────────────────────────────────────────┐
│         Main Process (Node.js)          │
│  ┌────────────────────────────────────┐ │
│  │  • Управление окнами               │ │
│  │  • База данных SQLite              │ │
│  │  • Файловая система                │ │
│  │  • Системные уведомления           │ │
│  │  • Автообновление                  │ │
│  └────────────────────────────────────┘ │
└──────────────────┬──────────────────────┘
                   │ IPC Bridge
                   │ (contextBridge)
┌──────────────────┴──────────────────────┐
│      Renderer Process (Chromium)        │
│  ┌────────────────────────────────────┐ │
│  │  • React UI                        │ │
│  │  • TanStack Query                  │ │
│  │  • Локальное состояние             │ │
│  │  • Взаимодействие с пользователем  │ │
│  └────────────────────────────────────┘ │
└─────────────────────────────────────────┘
```

### 2. Безопасность

- **Context Isolation** включен — renderer не имеет прямого доступа к Node.js
- **Node Integration** отключен — защита от XSS атак
- **Preload Script** — единственный мост между процессами
- **Content Security Policy** — ограничение загрузки внешних ресурсов
- **Sandbox** — renderer процесс работает в изолированной среде

### 3. Оффлайн-первый подход

- Все данные хранятся локально в SQLite
- Не требуется подключение к интернету для работы
- Данные не покидают устройство пользователя
- Автоматические резервные копии

## Структура проекта

```
equipment-tracker/
├── electron/                 # Main Process (Node.js)
│   ├── main.ts              # Точка входа приложения
│   ├── window.ts            # Управление окнами
│   ├── tray.ts              # Системный трей
│   ├── updater.ts           # Автообновление
│   ├── database.ts          # Подключение к SQLite
│   ├── migrations.ts        # Миграции БД
│   ├── notifications.ts     # Системные уведомления
│   ├── windowState.ts       # Сохранение состояния окна
│   ├── preload.ts           # IPC Bridge
│   └── ipc/                 # IPC обработчики
│       ├── requests.ts      # API заявок
│       ├── employeeExits.ts # API выходов
│       ├── backup.ts        # API резервных копий
│       └── templates.ts     # API шаблонов
│
├── src/                     # Renderer Process (React)
│   ├── main.tsx            # Точка входа React
│   ├── App.tsx             # Главный компонент
│   ├── index.css           # Глобальные стили
│   ├── components/         # React компоненты
│   │   ├── Dashboard.tsx
│   │   ├── RequestsView.tsx
│   │   ├── EmployeeExitView.tsx
│   │   ├── TemplatesView.tsx
│   │   ├── Sidebar.tsx
│   │   └── ui/             # shadcn/ui компоненты
│   ├── hooks/              # Custom React hooks
│   │   ├── useRequests.ts
│   │   ├── useEmployeeExits.ts
│   │   ├── useTemplates.ts
│   │   └── useDebounce.ts
│   ├── types/              # TypeScript типы
│   │   ├── electron.d.ts   # Типы для preload API
│   │   └── ipc.ts          # Zod схемы для IPC
│   └── lib/                # Утилиты
│       └── utils.ts
│
├── build/                   # Ресурсы для сборки
│   ├── icon.ico            # Windows иконка
│   ├── icon.icns           # macOS иконка
│   ├── icon.png            # Linux иконка
│   └── entitlements.mac.plist
│
├── .github/
│   └── workflows/
│       └── release.yml     # CI/CD pipeline
│
├── dist/                   # Production React bundle
├── dist-electron/          # Production Electron bundle
└── release/                # Готовые установщики
```

## Жизненный цикл приложения

### Запуск

1. **Инициализация Main Process** (`electron/main.ts`)

   ```
   app.on('ready') →
     Проверка единственного экземпляра →
     Инициализация базы данных →
     Применение миграций →
     Создание окна →
     Запуск планировщика уведомлений →
     Проверка обновлений
   ```

2. **Загрузка Renderer Process**
   ```
   Создание BrowserWindow →
     Загрузка index.html →
     Инициализация React →
     Инициализация TanStack Query →
     Загрузка начальных данных →
     Рендеринг UI
   ```

### Взаимодействие с данными

```
React Component
    ↓
TanStack Query Hook (useQuery/useMutation)
    ↓
window.electronAPI (preload)
    ↓
IPC Channel (ipcRenderer.invoke)
    ↓
IPC Handler (ipcMain.handle)
    ↓
Database Query (Knex)
    ↓
SQLite Database
    ↓
Response
    ↓
IPC Return
    ↓
Query Cache Update
    ↓
React Re-render
```

### Завершение работы

1. **Закрытие окна**
   - Windows/macOS: окно скрывается, приложение остаётся в трее
   - Linux: приложение закрывается полностью

2. **Выход из приложения** (через трей)
   ```
   app.on('before-quit') →
     Создание автоматической резервной копии →
     Закрытие соединения с БД →
     Завершение всех процессов
   ```

## Компоненты системы

### Main Process Components

#### 1. Window Manager (`window.ts`)

- Создание и управление главным окном
- Установка CSP заголовков
- Сохранение/восстановление позиции и размера окна
- Управление DevTools

#### 2. Database Manager (`database.ts`)

- Инициализация SQLite подключения через Knex
- Создание схемы БД при первом запуске
- Применение миграций
- Заполнение демо-данными в dev-режиме

#### 3. IPC Handlers (`ipc/`)

- Обработка запросов из renderer процесса
- Валидация входных данных через Zod
- Выполнение операций с БД
- Возврат типизированных ответов

#### 4. Notification Scheduler (`notifications.ts`)

- Периодическая проверка предстоящих выходов
- Отправка системных уведомлений
- 4 временных слота в день: 09:00, 12:00, 15:00, 18:00

#### 5. Auto-updater (`updater.ts`)

- Проверка обновлений при запуске
- Периодическая проверка каждые 4 часа
- Автоматическая загрузка обновлений
- Уведомления о доступных обновлениях

#### 6. Tray Manager (`tray.ts`)

- Иконка в системном трее
- Контекстное меню
- Показ/скрытие окна
- Выход из приложения

### Renderer Process Components

#### 1. State Management

- **TanStack Query** для серверного состояния (запросы к БД)
- **React State** для локального состояния UI
- **LocalStorage** для персистентных настроек (тема, размер sidebar)

#### 2. View Components

- **Dashboard** — главная панель со статистикой
- **RequestsView** — список заявок на оборудование
- **EmployeeExitView** — календарь и таблица выходов
- **TemplatesView** — шаблоны для быстрого создания заявок

#### 3. Modal Components

- **AddRequestModal** — создание новой заявки
- **EditRequestModal** — редактирование заявки
- **AddEmployeeExitModal** — создание выхода
- **EditEmployeeExitModal** — редактирование выхода
- **SettingsModal** — настройки и управление бэкапами

#### 4. Shared Components

- **SearchAndFilters** — поиск и фильтрация данных
- **TableSkeleton** — skeleton loader для таблиц
- **Sidebar** — боковая панель навигации
- **ThemeToggle** — переключатель темы

## Модель данных

### Основные сущности

#### 1. Request (Заявка)

```typescript
interface Request {
  id: number
  employee_name: string
  login: string
  sd_number: string
  created_at: string
  is_issued: boolean
  return_required: boolean
  return_completed: boolean
  return_date: string | null
  equipment_items: EquipmentItem[]
}
```

#### 2. EquipmentItem (Единица оборудования)

```typescript
interface EquipmentItem {
  id: number
  request_id: number
  equipment_name: string
  serial_number: string
}
```

#### 3. EmployeeExit (Выход сотрудника)

```typescript
interface EmployeeExit {
  id: number
  employee_name: string
  exit_date: string
  equipment_list: string // JSON или текст
  is_completed: boolean
  created_at: string
}
```

#### 4. Template (Шаблон заявки)

```typescript
interface Template {
  id: number
  name: string
  equipment_items: string // JSON массив
  created_at: string
}
```

### Связи между сущностями

```
Request (1) ←→ (N) EquipmentItem
   ↑
   │ (заявки связаны с оборудованием)
   │
EmployeeExit (независимая сущность)
   ↑
   │ (выходы отслеживаются отдельно)
   │
Template (используется для создания заявок)
```

## Потоки данных

### Создание заявки

```
1. Пользователь нажимает "Добавить заявку"
   ↓
2. Открывается AddRequestModal
   ↓
3. Пользователь заполняет форму
   - ФИО сотрудника
   - Логин
   - Номер заявки
   - Список оборудования (название + серийник)
   ↓
4. Нажатие "Создать"
   ↓
5. Валидация на клиенте (Zod)
   ↓
6. Отправка через window.electronAPI.createRequest()
   ↓
7. IPC Handler в electron/ipc/requests.ts
   ↓
8. Валидация на сервере (Zod)
   ↓
9. Транзакция в БД:
   - INSERT в таблицу requests
   - INSERT в таблицу equipment_items
   ↓
10. Возврат созданной заявки
   ↓
11. TanStack Query инвалидирует кэш ['requests']
   ↓
12. Автоматическое обновление списка заявок
   ↓
13. Toast-уведомление об успехе
```

### Система уведомлений о выходах

```
1. Приложение запускается
   ↓
2. Инициализация планировщика (notifications.ts)
   ↓
3. Установка таймера на проверку каждую минуту
   ↓
4. Проверка текущего времени
   ↓
5. Если время = 09:00, 12:00, 15:00 или 18:00:
   ↓
6. Запрос предстоящих выходов на сегодня и завтра
   ↓
7. Фильтрация незавершённых выходов
   ↓
8. Отправка системных уведомлений
   ↓
9. Сохранение timestamp последнего уведомления
```

### Автообновление

```
1. Приложение запускается (только в production)
   ↓
2. Проверка обновлений на GitHub Releases
   ↓
3. Сравнение версий (семантическое версионирование)
   ↓
4. Если новая версия доступна:
   ↓
5. Автоматическая загрузка в фоне
   ↓
6. Уведомление пользователя
   ↓
7. Пользователь выбирает:
   - Установить сейчас (перезапуск)
   - Установить при следующем запуске
   ↓
8. Установка обновления
```

## Безопасность и производительность

### Безопасность

1. **Изоляция процессов**
   - Renderer не имеет доступа к Node.js API
   - Все операции с файлами/БД через IPC

2. **Валидация данных**
   - Zod схемы на обеих сторонах
   - Санитизация пользовательского ввода

3. **CSP Headers**

   ```typescript
   'default-src': "'self'",
   'script-src': "'self'",
   'style-src': "'self' 'unsafe-inline'",
   'img-src': "'self' data: https:"
   ```

4. **Хранение данных**
   - Локальная SQLite БД
   - Данные не покидают устройство
   - Резервные копии в локальной файловой системе

### Производительность

1. **Оптимизация рендеринга**
   - React.memo для сложных компонентов
   - useMemo для вычисляемых значений
   - useCallback для стабильных функций

2. **Кеширование**
   - TanStack Query автоматически кеширует запросы
   - Оптимистичные обновления для быстрого отклика
   - Инвалидация кэша при мутациях

3. **Debouncing**
   - Поиск с задержкой 300ms
   - Уменьшение количества запросов к БД

4. **Skeleton loaders**
   - Показ placeholder'ов во время загрузки
   - Улучшение воспринимаемой производительности

## Обработка ошибок

### Стратегия обработки ошибок

1. **Main Process**

   ```typescript
   try {
     // Операция с БД
   } catch (error) {
     console.error('Ошибка:', error)
     return {
       success: false,
       error: 'Понятное сообщение на русском',
     }
   }
   ```

2. **Renderer Process**

   ```typescript
   const mutation = useMutation({
     onError: (error) => {
       toast.error(error.message)
     },
   })
   ```

3. **Глобальная обработка**
   - Логи пишутся в файл через electron-log
   - Критические ошибки показываются пользователю
   - Некритические ошибки логируются незаметно

## Расширяемость

### Добавление новой функции

1. **Определение требований**
   - Какие данные нужны?
   - Какие операции необходимы?

2. **Обновление БД**
   - Создание миграции в `electron/migrations.ts`
   - Обновление схемы в `electron/database.ts`

3. **Создание IPC API**
   - Добавление Zod схемы в `src/types/ipc.ts`
   - Создание handlers в `electron/ipc/`
   - Экспорт API в `electron/preload.ts`

4. **Типизация**
   - Обновление `src/types/electron.d.ts`

5. **React интеграция**
   - Создание custom hook с TanStack Query
   - Создание UI компонентов

6. **Тестирование**
   - Ручное тестирование функциональности
   - Проверка на разных платформах

## Лучшие практики

### 1. Именование

- **Файлы**: kebab-case для утилит, PascalCase для компонентов
- **Компоненты**: PascalCase
- **Функции/переменные**: camelCase
- **Константы**: UPPER_SNAKE_CASE

### 2. Организация кода

- Один компонент — один файл
- Общие компоненты в `components/`
- UI примитивы в `components/ui/`
- Hooks в `hooks/`
- Утилиты в `lib/`

### 3. TypeScript

- Строгий режим включен
- Избегать `any`
- Использовать Zod для runtime валидации
- Типы для IPC API всегда синхронизированы

### 4. React

- Функциональные компоненты
- Hooks для состояния и эффектов
- TanStack Query для серверных данных
- LocalStorage для персистентных настроек

### 5. Стилизация

- Tailwind CSS для утилитарных классов
- CSS переменные для темизации
- shadcn/ui для готовых компонентов
- Адаптивный дизайн

## Заключение

Архитектура Equipment Tracker спроектирована для:

- **Безопасности**: изоляция процессов, валидация данных
- **Производительности**: оптимизация рендеринга, кеширование
- **Надёжности**: оффлайн-работа, автоматические бэкапы
- **Расширяемости**: модульная структура, чёткое разделение ответственности
- **Поддерживаемости**: TypeScript, линтинг, форматирование

Следование этим принципам гарантирует стабильность и качество приложения.
