# 🔧 Решение типичных проблем

## Установка и запуск

### Приложение не запускается

**Симптомы:**

- Двойной клик по иконке ничего не делает
- Приложение открывается и сразу закрывается
- Белый экран при запуске

**Решение:**

1. **Проверьте процессы**

   ```bash
   # Windows
   tasklist | findstr "Equipment Tracker"

   # Если процесс висит, завершите его
   taskkill /F /IM "Equipment Tracker.exe"
   ```

2. **Проверьте логи**
   - Windows: `%USERPROFILE%\AppData\Roaming\Equipment Tracker\logs\`
   - macOS: `~/Library/Logs/Equipment Tracker/`
   - Linux: `~/.config/Equipment Tracker/logs/`

3. **Переустановите приложение**
   - Удалите текущую версию
   - Скачайте последнюю версию с GitHub
   - Установите заново

4. **Проверьте антивирус**
   - Добавьте приложение в исключения
   - Временно отключите антивирус для проверки

---

### Белый экран при запуске

**Причины:**

- Повреждённый кэш
- Ошибка в renderer процессе
- Проблемы с БД

**Решение:**

1. **Очистите кэш приложения**

   ```bash
   # Windows
   del /F /Q "%APPDATA%\equipment-tracker\Cache\*"
   del /F /Q "%APPDATA%\equipment-tracker\GPUCache\*"
   ```

2. **Проверьте DevTools (в режиме разработки)**
   - Запустите `npm run dev`
   - Проверьте консоль на наличие ошибок

3. **Восстановите БД из резервной копии**

   ```bash
   # Найдите последнюю резервную копию
   %APPDATA%\equipment-tracker\backups\

   # Восстановите через UI или вручную
   copy backup-latest.db equipment.db
   ```

---

### macOS: "Equipment Tracker" не может быть открыт

**Ошибка:**

```
"Equipment Tracker.app" cannot be opened because the developer cannot be verified
```

**Решение:**

1. **Метод 1: Через Security & Privacy**
   - Откройте System Preferences → Security & Privacy
   - Нажмите "Open Anyway"

2. **Метод 2: Через Terminal**

   ```bash
   xattr -cr "/Applications/Equipment Tracker.app"
   ```

3. **Метод 3: Разрешить установку из любых источников**
   ```bash
   sudo spctl --master-disable
   ```
   ⚠️ Не забудьте включить обратно после установки!

---

### Linux: AppImage не запускается

**Решение:**

1. **Дайте права на выполнение**

   ```bash
   chmod +x Equipment-Tracker-*.AppImage
   ```

2. **Установите FUSE**

   ```bash
   # Ubuntu/Debian
   sudo apt install fuse libfuse2

   # Fedora
   sudo dnf install fuse fuse-libs

   # Arch
   sudo pacman -S fuse2
   ```

3. **Запустите из терминала для просмотра ошибок**
   ```bash
   ./Equipment-Tracker-*.AppImage
   ```

---

## База данных

### База данных заблокирована

**Ошибка:**

```
SQLITE_BUSY: database is locked
```

**Причины:**

- Запущено несколько экземпляров приложения
- Предыдущий процесс не завершился корректно
- Антивирус блокирует файл

**Решение:**

1. **Закройте все экземпляры приложения**

   ```bash
   # Windows
   taskkill /F /IM "Equipment Tracker.exe"

   # Linux/macOS
   pkill -9 "Equipment Tracker"
   ```

2. **Удалите файлы блокировки**

   ```bash
   # Windows
   del "%APPDATA%\equipment-tracker\equipment.db-shm"
   del "%APPDATA%\equipment-tracker\equipment.db-wal"
   ```

3. **Перезапустите приложение**

---

### База данных повреждена

**Симптомы:**

- Ошибки при загрузке данных
- Приложение не запускается
- Сообщение "malformed database"

**Решение:**

1. **Попробуйте восстановить целостность**

   ```bash
   sqlite3 equipment.db "PRAGMA integrity_check;"
   ```

2. **Восстановите из резервной копии**
   - Откройте Settings → Restore Backup
   - Выберите последнюю копию из `backups/`

3. **Используйте emergency backup**

   ```bash
   # Если есть equipment.db.emergency
   copy equipment.db equipment.db.broken
   copy equipment.db.emergency equipment.db
   ```

4. **Экспортируйте данные (если БД частично работает)**
   ```bash
   sqlite3 equipment.db ".dump" > backup.sql
   ```

---

### Данные не сохраняются

**Проверьте:**

1. **Права доступа к папке**

   ```bash
   # Windows
   icacls "%APPDATA%\equipment-tracker"

   # Linux/macOS
   ls -la ~/.config/equipment-tracker/
   ```

2. **Доступное место на диске**

   ```bash
   # Windows
   wmic logicaldisk get size,freespace,caption
   ```

3. **Логи приложения на наличие ошибок**

4. **Антивирус не блокирует запись**
   - Добавьте папку в исключения

---

## Автообновление

### Обновления не находятся

**Причины:**

- Нет подключения к интернету
- GitHub недоступен
- Используется dev версия

**Решение:**

1. **Проверьте подключение**

   ```bash
   ping github.com
   ```

2. **Проверьте версию**
   - Settings → About
   - Убедитесь, что не используется dev версия

3. **Принудительно проверьте обновления**
   - Перезапустите приложение
   - Обновление проверяется автоматически

4. **Ручная установка**
   - Скачайте с https://github.com/MaRT1n1q/Equipment.Tracker/releases
   - Установите вручную

---

### Обновление загружено, но не устанавливается

**Решение:**

1. **Закройте все окна приложения**
   - Выйдите через трей

2. **Перезапустите приложение**
   - Обновление установится автоматически

3. **Проверьте права администратора**
   - Windows: запустите от имени администратора
   - macOS: проверьте права в Applications

---

## Производительность

### Приложение работает медленно

**Причины:**

- Большой объём данных
- Недостаточно ресурсов
- Фоновые процессы

**Решение:**

1. **Очистите старые данные**
   - Удалите неактуальные заявки
   - Архивируйте завершённые выходы

2. **Проверьте ресурсы**

   ```bash
   # Windows
   taskmgr

   # Linux
   htop
   ```

3. **Увеличьте выделенную память**
   - Закройте другие приложения
   - Перезагрузите компьютер

4. **Оптимизируйте БД**
   ```bash
   sqlite3 equipment.db "VACUUM;"
   ```

---

### Поиск работает медленно

**Решение:**

1. **Используйте более специфичные запросы**
   - Вводите минимум 3 символа
   - Используйте фильтры

2. **Проверьте индексы БД**

   ```sql
   SELECT * FROM sqlite_master WHERE type='index';
   ```

3. **Пересоздайте индексы**
   - Будет исправлено в следующем обновлении
   - Или переустановите приложение

---

## Экспорт данных

### Экспорт CSV не работает

**Ошибка:**

- Диалог не открывается
- Файл не создаётся
- Приложение зависает

**Решение:**

1. **Проверьте права записи**
   - Выберите папку, где у вас есть права
   - Попробуйте другую папку

2. **Закройте другие программы**
   - Особенно Excel, если планируете открыть CSV в нём

3. **Уменьшите объём данных**
   - Используйте фильтры перед экспортом
   - Экспортируйте по частям

4. **Проверьте логи**
   - Посмотрите детали ошибки

---

## Резервные копии

### Не удаётся создать резервную копию

**Причины:**

- Недостаточно места на диске
- Нет прав доступа
- БД заблокирована

**Решение:**

1. **Освободите место**

   ```bash
   # Удалите старые резервные копии
   # Папка: %APPDATA%\equipment-tracker\backups\
   ```

2. **Выберите другую папку**
   - При создании ручной копии
   - Выберите папку с правами записи

3. **Закройте приложение полностью**
   - Выйдите через трей
   - Подождите 5 секунд
   - Запустите снова

---

### Не удаётся восстановить резервную копию

**Ошибка:**

- "Invalid database file"
- "Restore failed"

**Решение:**

1. **Проверьте файл резервной копии**

   ```bash
   sqlite3 backup.db "PRAGMA integrity_check;"
   ```

2. **Используйте другую копию**
   - Попробуйте более раннюю копию
   - Проверьте папку автоматических копий

3. **Ручное восстановление**
   ```bash
   # Закройте приложение
   # Скопируйте файл вручную
   copy backup.db "%APPDATA%\equipment-tracker\equipment.db"
   ```

---

## Интерфейс

### Тёмная тема не работает

**Решение:**

1. **Переключите тему вручную**
   - Кликните на иконку солнца/луны
   - Настройки сохраняются автоматически

2. **Очистите кэш браузера**

   ```bash
   # В DevTools: F12
   # Application → Clear storage → Clear site data
   ```

3. **Проверьте LocalStorage**
   - F12 → Application → LocalStorage
   - Ключ: `equipment-tracker:theme`
   - Значение: `dark` или `light`

---

### Элементы UI наезжают друг на друга

**Причины:**

- Нестандартный масштаб Windows
- Проблемы с Tailwind CSS

**Решение:**

1. **Проверьте масштабирование**
   - Windows: Settings → Display → Scale
   - Рекомендуется: 100% или 125%

2. **Обновите приложение**
   - Скачайте последнюю версию

3. **Измените zoom**
   - Ctrl + 0 (сбросить zoom)
   - Ctrl + Mouse Wheel (настроить)

---

## Разработка

### `npm install` не работает

**Ошибка:**

- gyp ERR!
- node-gyp rebuild failed

**Решение:**

1. **Установите build tools**

   ```bash
   # Windows
   npm install --global windows-build-tools

   # macOS
   xcode-select --install

   # Linux (Ubuntu/Debian)
   sudo apt install build-essential
   ```

2. **Используйте правильную версию Node.js**

   ```bash
   node --version  # Должна быть 18+
   ```

3. **Очистите кэш npm**
   ```bash
   npm cache clean --force
   rm -rf node_modules package-lock.json
   npm install
   ```

---

### Hot reload не работает

**Решение:**

1. **Перезапустите dev сервер**

   ```bash
   # Ctrl+C для остановки
   npm run dev
   ```

2. **Проверьте порт 5173**

   ```bash
   # Windows
   netstat -ano | findstr :5173

   # Если занят, завершите процесс
   taskkill /PID <PID> /F
   ```

3. **Измените файл снова**
   - Иногда первое изменение не подхватывается

---

### Сборка завершается ошибкой

**Ошибка:**

```
app.asar is being used by another process
```

**Решение:**

1. **Закройте все экземпляры приложения**
   - Включая те, что в трее
   - Проверьте Task Manager

2. **Удалите папки build**

   ```bash
   rm -rf dist dist-electron release
   npm run build
   ```

3. **Перезагрузите компьютер**
   - Если ничего не помогает

---

## Логирование и отладка

### Как включить подробное логирование

**Main Process:**

```typescript
// electron/main.ts
import log from 'electron-log'
log.transports.file.level = 'debug'
```

**Renderer Process:**

- F12 → Console
- Все console.log видны здесь

**SQLite:**

```typescript
// electron/database.ts
const knex = Knex({
  debug: true, // Логирует все SQL запросы
})
```

---

### Где найти логи

**Windows:**

```
%USERPROFILE%\AppData\Roaming\Equipment Tracker\logs\main.log
```

**macOS:**

```
~/Library/Logs/Equipment Tracker/main.log
```

**Linux:**

```
~/.config/Equipment Tracker/logs/main.log
```

---

## Получение помощи

### Прежде чем обратиться за помощью

Подготовьте следующую информацию:

1. **Версия приложения**
   - Settings → About

2. **Операционная система**
   - Windows 10/11
   - macOS версия
   - Linux дистрибутив

3. **Шаги воспроизведения**
   - Что вы делали
   - Что произошло
   - Что ожидали

4. **Логи**
   - Последние строки из main.log
   - Скриншоты ошибок

5. **Скриншоты**
   - Показывают проблему

### Где получить помощь

1. **Документация**
   - [README.md](../README.md)
   - [USAGE.md](../USAGE.md)
   - [docs/](.)

2. **GitHub Issues**
   - https://github.com/MaRT1n1q/Equipment.Tracker/issues
   - Поищите существующие Issue
   - Создайте новый с деталями

3. **Discussions**
   - https://github.com/MaRT1n1q/Equipment.Tracker/discussions
   - Задайте вопрос сообществу

---

## Известные проблемы

### Windows Defender SmartScreen

**Предупреждение:**

```
Windows protected your PC
```

**Причина:**

- Приложение не подписано кодовым сертификатом

**Решение:**

- Нажмите "More info" → "Run anyway"
- Или подождите, пока приложение наберёт репутацию

---

### macOS Notarization

**Ошибка:**

```
"Equipment Tracker.app" is damaged and can't be opened
```

**Причина:**

- Приложение не нотаризовано Apple

**Решение:**

```bash
xattr -cr "/Applications/Equipment Tracker.app"
```

---

## Обратная связь

Если это руководство не помогло решить вашу проблему:

1. Создайте [Issue](https://github.com/MaRT1n1q/Equipment.Tracker/issues)
2. Опишите проблему детально
3. Приложите логи и скриншоты

Мы постараемся помочь как можно скорее!
