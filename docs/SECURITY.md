# 🔒 Политика безопасности

## Поддерживаемые версии

Мы предоставляем обновления безопасности для следующих версий Equipment Tracker:

| Версия | Поддержка          | Статус              |
| ------ | ------------------ | ------------------- |
| 1.0.x  | :white_check_mark: | Активная разработка |
| < 1.0  | :x:                | Не поддерживается   |

**Рекомендация:** Всегда используйте последнюю версию приложения.

## Архитектура безопасности

### Изоляция процессов

Equipment Tracker следует лучшим практикам безопасности Electron:

#### Context Isolation

- **Включено**: `contextIsolation: true`
- Renderer процесс изолирован от Node.js API
- Нет прямого доступа к системным функциям из UI

#### Node Integration

- **Отключено**: `nodeIntegration: false`
- Защита от выполнения произвольного Node.js кода
- Предотвращение XSS атак

#### Sandbox

- **Включено**: `sandbox: true`
- Renderer процесс работает в ограниченной среде
- Дополнительный уровень защиты

### IPC Безопасность

#### Preload Bridge

- Единственный канал связи между процессами
- Белый список разрешённых методов
- Нет доступа к `ipcRenderer` напрямую

```typescript
// ✅ Безопасно: через preload
window.electronAPI.getRequests()

// ❌ Небезопасно: прямой доступ (заблокирован)
require('electron').ipcRenderer
```

#### Валидация данных

Двухуровневая валидация:

1. **Renderer** — Zod схемы перед отправкой
2. **Main** — повторная валидация в IPC handlers

```typescript
// Пример валидации
const data = createRequestSchema.parse(payload)
```

### Content Security Policy

Строгий CSP предотвращает загрузку вредоносного контента:

```typescript
'default-src': "'self'",
'script-src': "'self'",
'style-src': "'self' 'unsafe-inline'",
'img-src': "'self' data: https:"
```

### Хранение данных

#### Локальное хранение

- Все данные хранятся локально в SQLite
- Данные не передаются в интернет
- Нет внешних API вызовов

#### Резервные копии

- Автоматические локальные бэкапы
- Emergency backup перед критическими операциями
- Хранятся в папке пользователя

#### Путь к данным

- **Windows**: `%APPDATA%\equipment-tracker\`
- **macOS**: `~/Library/Application Support/equipment-tracker/`
- **Linux**: `~/.config/equipment-tracker/`

### Обновления

#### Безопасная доставка

- Обновления загружаются только через HTTPS
- Проверка цифровой подписи (если настроена)
- Только из официального GitHub Releases

#### Автообновление

- Работает только в production сборках
- Пользователь контролирует установку
- Автоматический бэкап перед обновлением

## Известные ограничения

### 1. Без удалённого доступа

Приложение не предоставляет удалённый доступ к данным. Это feature, а не bug — обеспечивает приватность.

### 2. Локальные привилегии

Приложение работает с правами текущего пользователя. Не требует и не запрашивает повышенные привилегии.

### 3. Нет шифрования БД

SQLite база данных не зашифрована. Если требуется шифрование:

- Используйте BitLocker (Windows)
- FileVault (macOS)
- LUKS (Linux)

## Сообщение об уязвимостях

### Как сообщить

Если вы обнаружили уязвимость:

1. **НЕ создавайте публичный Issue**
2. Отправьте отчёт на email: **support@equipment-tracker.app**
3. Или через [Security Advisories](https://github.com/MaRT1n1q/Equipment.Tracker/security/advisories)

### Что включить в отчёт

```
Тема: [SECURITY] Краткое описание уязвимости

Описание:
- Тип уязвимости (XSS, RCE, SQLi, etc.)
- Затронутая версия
- Шаги для воспроизведения
- Потенциальное воздействие
- Предлагаемое решение (если есть)

Дополнительная информация:
- Proof of Concept (если безопасно)
- Скриншоты
- Логи
```

### Процесс обработки

1. **Подтверждение получения** — в течение 48 часов
2. **Первичная оценка** — 1-5 дней
3. **Разработка исправления** — зависит от серьёзности
4. **Релиз патча** — публикация обновления
5. **Раскрытие информации** — публикация advisory

### Timeline

| Серьёзность | Целевое время исправления |
| ----------- | ------------------------- |
| Critical    | 1-3 дня                   |
| High        | 1-2 недели                |
| Medium      | 2-4 недели                |
| Low         | Следующий релиз           |

### Классификация серьёзности

**Critical:**

- Удалённое выполнение кода (RCE)
- Полная компрометация системы
- Утечка конфиденциальных данных

**High:**

- Локальное выполнение кода
- Privilege escalation
- Обход аутентификации

**Medium:**

- XSS в изолированном контексте
- DoS атаки
- Information disclosure

**Low:**

- Незначительные утечки информации
- UI spoofing
- Неопасные race conditions

## Security Checklist

### Для разработчиков

При внесении изменений проверьте:

- [ ] Context Isolation остаётся включенным
- [ ] Node Integration остаётся отключенным
- [ ] Все IPC методы валидируют входные данные
- [ ] Нет eval(), new Function(), innerHTML
- [ ] Нет динамических require() в renderer
- [ ] CSP не ослаблен
- [ ] Нет утечек чувствительных данных в логи
- [ ] Файловые операции используют безопасные пути

### Для пользователей

Как защитить свои данные:

- [ ] Скачивайте только с официального GitHub
- [ ] Проверяйте цифровую подпись установщика
- [ ] Держите приложение обновлённым
- [ ] Используйте шифрование диска ОС
- [ ] Регулярно создавайте резервные копии
- [ ] Не запускайте от имени администратора
- [ ] Используйте антивирус

## Безопасная разработка

### Запрещённые паттерны

```typescript
// ❌ НИКОГДА не делайте так
eval(userInput)
new Function(userInput)()
element.innerHTML = userInput
require(userInput)
```

### Безопасные альтернативы

```typescript
// ✅ Делайте так
JSON.parse(userInput) // с try-catch
element.textContent = userInput
// Белый список для require
```

### Валидация

Всегда валидируйте:

- Пользовательский ввод
- Данные из БД
- IPC payload
- Файловые пути

```typescript
// Пример безопасной валидации
const data = schema.parse(payload) // Zod
const safePath = path.join(userDataPath, sanitizedName)
```

## Ответственное раскрытие

Мы просим исследователей безопасности:

1. **Не эксплуатировать** уязвимость за пределами тестирования
2. **Не раскрывать** публично до выхода патча
3. **Дать время** на исправление (90 дней)
4. **Уведомить нас** перед публикацией
5. **Не тестировать** на production данных пользователей

### Благодарности

Мы публично благодарим исследователей, ответственно раскрывших уязвимости (с их согласия).

## Hall of Fame

_Здесь будут перечислены исследователи, помогшие улучшить безопасность проекта._

<!-- Формат:
- **Имя** - описание уязвимости - дата
-->

## Дополнительная информация

### Документация

- [Electron Security](https://www.electronjs.org/docs/latest/tutorial/security)
- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [CWE Top 25](https://cwe.mitre.org/top25/)

### Контакты

- **Email**: support@equipment-tracker.app
- **GitHub Security**: [Security Advisories](https://github.com/MaRT1n1q/Equipment.Tracker/security/advisories)

---

**Безопасность — наш приоритет. Спасибо за помощь в защите пользователей! 🔒**
