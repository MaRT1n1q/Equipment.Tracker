name: Build and Release

on:
  push:
    tags:
      - 'v*.*.*' # Запускается при пуше тегов типа v1.0.0, v1.0.1 и т.д.

permissions:
  contents: write # Разрешаем создание релизов

jobs:
  # Билд для Windows
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Clean release folder
        run: Remove-Item -Recurse -Force -ErrorAction SilentlyContinue release
        shell: pwsh

      - name: Build Windows application
        run: npm run build -- --win
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v4
        with:
          name: windows-release
          path: |
            release/*.exe
            release/*.exe.blockmap
            release/latest.yml
          retention-days: 1

  # Билд для macOS
  build-macos:
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Clean release folder
        run: rm -rf release

      - name: Build macOS application
        run: npm run build -- --mac
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload macOS artifacts
        uses: actions/upload-artifact@v4
        with:
          name: macos-release
          path: |
            release/*.dmg
            release/*.dmg.blockmap
            release/latest-mac.yml
          retention-days: 1

  # Билд для Linux
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Clean release folder
        run: rm -rf release

      - name: Build Linux application
        run: npm run build -- --linux
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Linux artifacts
        uses: actions/upload-artifact@v4
        with:
          name: linux-release
          path: |
            release/*.AppImage
            release/*.AppImage.blockmap
            release/latest-linux.yml
          retention-days: 1

  # Создание релиза со всеми платформами
  create-release:
    needs: [build-windows, build-macos, build-linux]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Get version from tag
        id: get_version
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT

      - name: Download Windows artifacts
        uses: actions/download-artifact@v4
        with:
          name: windows-release
          path: ./release-windows

      - name: Download macOS artifacts
        uses: actions/download-artifact@v4
        with:
          name: macos-release
          path: ./release-macos

      - name: Download Linux artifacts
        uses: actions/download-artifact@v4
        with:
          name: linux-release
          path: ./release-linux

      - name: Create Release with all platforms
        uses: softprops/action-gh-release@v2
        with:
          name: 🚀 Equipment Tracker v${{ steps.get_version.outputs.VERSION }}
          body: |
            # 🚀 Релиз Equipment Tracker v${{ steps.get_version.outputs.VERSION }}


            ---

            ## 📥 Установка

            ### 🪟 Windows (x64)

            [![Download EXE](https://img.shields.io/badge/💾_Скачать-.exe-blue?style=for-the-badge)](https://github.com/MaRT1n1q/Equipment.Tracker/releases/download/v${{ steps.get_version.outputs.VERSION }}/Equipment-Tracker-${{ steps.get_version.outputs.VERSION }}-win-x64.exe)

            ▶️ Запустите установщик и следуйте инструкциям

            ---

            ### 🍏 macOS

            **Intel (x64):**

            [![Download DMG Intel](https://img.shields.io/badge/💾_Скачать-.dmg-brightgreen?style=for-the-badge)](https://github.com/MaRT1n1q/Equipment.Tracker/releases/download/v${{ steps.get_version.outputs.VERSION }}/Equipment-Tracker-${{ steps.get_version.outputs.VERSION }}-mac-x64.dmg) [![Download ZIP Intel](https://img.shields.io/badge/💾_Скачать-.zip-green?style=for-the-badge)](https://github.com/MaRT1n1q/Equipment.Tracker/releases/download/v${{ steps.get_version.outputs.VERSION }}/Equipment-Tracker-${{ steps.get_version.outputs.VERSION }}-mac-x64.zip)

            **Apple Silicon (arm64):**

            [![Download DMG ARM](https://img.shields.io/badge/💾_Скачать-.dmg-brightgreen?style=for-the-badge)](https://github.com/MaRT1n1q/Equipment.Tracker/releases/download/v${{ steps.get_version.outputs.VERSION }}/Equipment-Tracker-${{ steps.get_version.outputs.VERSION }}-mac-arm64.dmg) [![Download ZIP ARM](https://img.shields.io/badge/💾_Скачать-.zip-green?style=for-the-badge)](https://github.com/MaRT1n1q/Equipment.Tracker/releases/download/v${{ steps.get_version.outputs.VERSION }}/Equipment-Tracker-${{ steps.get_version.outputs.VERSION }}-mac-arm64.zip)

            📂 Откройте `.dmg` и перетащите приложение в папку **Applications**

            ---

            ### 🐧 Linux (x64)

            [![Download AppImage](https://img.shields.io/badge/💾_Скачать-.AppImage-orange?style=for-the-badge)](https://github.com/MaRT1n1q/Equipment.Tracker/releases/download/v${{ steps.get_version.outputs.VERSION }}/Equipment-Tracker-${{ steps.get_version.outputs.VERSION }}-linux-x64.AppImage) [![Download DEB](https://img.shields.io/badge/💾_Скачать-.deb-red?style=for-the-badge)](https://github.com/MaRT1n1q/Equipment.Tracker/releases/download/v${{ steps.get_version.outputs.VERSION }}/Equipment-Tracker-${{ steps.get_version.outputs.VERSION }}-linux-x64.deb)

            **Для AppImage:**
            ```bash
            chmod +x Equipment-Tracker-${{ steps.get_version.outputs.VERSION }}-linux-x64.AppImage
            ./Equipment-Tracker-${{ steps.get_version.outputs.VERSION }}-linux-x64.AppImage
            ```

            ---

            ## 🔄 Автообновление

            Если у вас уже установлена предыдущая версия, приложение **автоматически обнаружит и установит обновление** — ничего делать не нужно 💫
          draft: false
          prerelease: false
          files: |
            ./release-windows/*
            ./release-macos/*
            ./release-linux/*
          fail_on_unmatched_files: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
